# app_budgets.yaml -- declared RAM footprint for every app compiled into the
# firmware. The budget gate (tools/port/budget_check.py) reads this on every PR.
#
# HOW TO FILL THIS IN (do not guess in anger -- measure):
#   static_bytes     : always-resident cost -- file-scope globals, BSS, non-const
#                      `static` members, RAM string tables. (const tables in
#                      flash/DROM cost 0 here.)
#   object_bytes     : sizeof(the Activity subclass) -- the heap allocation made
#                      when the screen is pushed. Get it from a static_assert or
#                      the .map file.
#   peak_heap_bytes  : the MOST heap this app holds at once while running. 0 if none.
#   tier             : c3 (must fit the base X4, enforced) | psram (X4 Pro only).
#
# Numbers below are first-pass estimates -- replace with measured values before
# each app ships. The gate is only as honest as this file.
#
# WHAT A "PASS" HERE MEANS: that the *declared* budget fits the ceiling. It does
# NOT mean the app exists. Most entries below were declared ahead of the port and
# the app is not in the tree yet -- see docs/merge/PORT_LEDGER.md for what has
# actually landed. Keep declaring budgets up front (it is the cheapest moment to
# notice an app will not fit), but do not read a green gate as a shipped app.
#
# FLASH: this manifest measures declared RAM, not the linked firmware image.
# PlatformIO already runs scripts/check_firmware_size.py against the smallest
# configured app slot. It fails on overflow and warns below the 256 KiB reserve;
# CROSSINK_FLASH_FAIL_UNDER_RESERVE=1 makes the reserve a hard failure.
# Batch 7 snapshot: 6,316,352-byte image / 6,553,600-byte slot, 237,248 bytes free.
# See docs/merge/verification/BATCH_6_FLASH_2026-09-09.md.
# Per-app deltas require controlled before/after builds; object-file averages
# are not additive flash budgets. Keep the recovery/expansion gate.

apps:
  # ---- Ported & wired into the Tools launcher (safe tier, no radio) ----
  - name: Calculator          # ported by v1, moved into src/ and re-audited; measured sizeof
    tier: c3
    static_bytes: 0
    object_bytes: 584
    peak_heap_bytes: 0

  - name: UnitConverter       # ported; nested vectors -> constexpr tables in flash; measured sizeof
    tier: c3
    static_bytes: 0
    object_bytes: 616
    peak_heap_bytes: 512

  - name: Snake                 # wip; fixed-capacity body allocation
    tier: c3
    static_bytes: 128
    object_bytes: 512           # conservative estimate, runtime validation deferred
    peak_heap_bytes: 6144       # fixed 1536 * 4-byte body storage

  - name: Minesweeper           # wip; fixed grid and bounded local reveal queue
    tier: c3
    static_bytes: 128
    object_bytes: 512           # grid[10][16] in-object
    peak_heap_bytes: 512

  - name: Sudoku                # wip; iterative solver, bounded generation
    tier: c3
    static_bytes: 128
    object_bytes: 768           # conservative estimate incl. solver arrays; runtime checks deferred
    peak_heap_bytes: 0

  - name: AppLauncher           # the tile grid; one instance per level (root, category)
    tier: c3
    static_bytes: 0             # the app table is constexpr -> flash, 0 DRAM
    object_bytes: 2628          # dominated by FreeInkApp<32,4>'s interaction buffer
    peak_heap_bytes: 1024       # ListItem vector, reserved to the row count

  # ---- Batch 2 (safe tier, no radio) ----
  - name: DiceRoller          # ported; vector -> fixed array, esp_random -> xorshift; measured sizeof
    tier: c3
    static_bytes: 0
    object_bytes: 260
    peak_heap_bytes: 0
  - name: Countdown           # ported; repaint gated on the seconds digit; measured sizeof
    tier: c3
    static_bytes: 0
    object_bytes: 252
    peak_heap_bytes: 0
  - name: MorseCode           # ported; std::string build -> caller buffers; measured sizeof
    tier: c3
    static_bytes: 0
    object_bytes: 680
    peak_heap_bytes: 512
  - name: Cipher                # ported; std::string -> Cipher.h pure functions on char[]; 16 host tests
    tier: c3
    static_bytes: 0              # no static/global members, same as every other ported app
    object_bytes: 664  # measured C3 factory allocation, 2026-09-05
    peak_heap_bytes: 1024       # unverified shared-keyboard peak; see batch 3 evidence
  - name: OtpGenerator          # ported; esp_random -> mbedtls_ctr_drbg (hardware only; simulator reports unavailable)
    tier: c3
    static_bytes: 0
    object_bytes: 832  # measured C3 factory allocation, 2026-09-05
    peak_heap_bytes: 256        # unverified hardware crypto-context peak; see batch 4 evidence
  - name: QrGenerator            # ported; reuses existing src/util/QrUtils.h, no new QR implementation
    tier: c3
    static_bytes: 0
    object_bytes: 488  # measured C3 factory allocation, 2026-09-05
    peak_heap_bytes: 4096       # unverified estimate incl. shared keyboard; see batch 2 evidence
  - name: Barcode              # wip; fixed payload, shared keyboard
    tier: c3
    static_bytes: 0
    object_bytes: 512          # conservative estimate
    peak_heap_bytes: 4096      # shared keyboard transient estimate; V1 measurement pending
  - name: Tetris
    tier: c3
    static_bytes: 128
    object_bytes: 512           # board in-object
    peak_heap_bytes: 0
  - name: Maze                # wip; one fallible lifecycle Storage allocation
    tier: c3
    static_bytes: 128
    object_bytes: 512          # conservative estimate
    peak_heap_bytes: 12302     # grid + visited + parents + shared work buffer
  - name: GameOfLife
    tier: c3
    static_bytes: 128
    object_bytes: 252  # measured C3 factory allocation, 2026-09-05
    peak_heap_bytes: 2048       # cell grid (double-buffered)

  - name: Clock                  # ported; offline-only against HalClock (no WiFi/NTP in the app itself,
                                  # resolving the v1 <WiFi.h> objection -- see docs/merge/PORT_LEDGER.md and
                                  # ClockActivity.h). Standard simulator HalClock reports isAvailable()==false, so
                                  # the smoke test exercises the "no RTC" fallback path, not the happy path.
    tier: c3
    static_bytes: 0
    object_bytes: 312  # measured C3 factory allocation, 2026-09-05
    peak_heap_bytes: 0

  # ---- Phase 3 recon tier (forward-looking examples; radio, capped buffers) ----
  - name: WifiScanner           # ported (radio tier), capped at MAX_NETWORKS=40 via RADIO
    tier: c3
    static_bytes: 512
    object_bytes: 2048
    peak_heap_bytes: 16384      # 40 entries * (2 std::string + fields); shrink with char[]

  - name: BleScanner            # ported; devices capped 50, services capped 32 (MEM-008 fix)
    tier: c3
    static_bytes: 512
    object_bytes: 2048
    peak_heap_bytes: 24576      # capped device/service/char lists
  - name: PacketMonitor         # ported; ISR-safe (callback -> RingBuffer -> loop), snap 256
    tier: c3
    static_bytes: 512
    object_bytes: 9216          # macTable[256]*8 (2KB) + pcapQueue 24*260 (6.2KB) in-object
    peak_heap_bytes: 2048       # PCAP streams to SD, not buffered in RAM

  # ---- Comms tier ----
  - name: MeshChat              # ported; fixed message ring + peers.reserve(20) (no callback alloc)
    tier: c3
    static_bytes: 256
    object_bytes: 6144          # messages[20]*(name+text) + peers[20] fixed/reserved
    peak_heap_bytes: 1024

  # ---- Defense tier ----
  - name: TrackerDetector       # ported; BLE results read synchronously, devices capped
    tier: c3
    static_bytes: 256
    object_bytes: 2048
    peak_heap_bytes: 8192       # capped tracked-device list
  - name: DeauthDetector        # ported; IRAM_ATTR callback, fixed ring, counters only
    tier: c3
    static_bytes: 256
    object_bytes: 1536
    peak_heap_bytes: 0          # fixed ring buffer, no heap

  - name: PacketCaptureFullFrame  # hypothetical full-frame (no snap) capture -> X4 Pro only
    tier: psram
    static_bytes: 512
    object_bytes: 4096
    peak_heap_bytes: 215040     # large full-frame ring; PSRAM only

  - name: EtchASketch          # wip; lower-resolution logical drawing, no extra display framebuffer
    tier: c3
    static_bytes: 0
    object_bytes: 512          # conservative estimate; runtime validation deferred
    peak_heap_bytes: 5824      # 4800 drawing + 1024 estimated HAL file transient

  - name: KeyCopier            # wip; charts only, flash metadata
    tier: c3
    static_bytes: 0
    object_bytes: 384          # conservative estimate
    peak_heap_bytes: 0

  - name: EventLogger         # wip; bounded recent-entry ring
    tier: c3
    static_bytes: 0
    object_bytes: 512         # conservative estimate
    peak_heap_bytes: 14792    # 6600 ring + 8192 estimated keyboard/HAL transient

  - name: Flashcards          # wip; fixed 32 cards and 16 names
    tier: c3
    static_bytes: 0
    object_bytes: 512         # conservative estimate
    peak_heap_bytes: 10756    # 9732 deck buffers + 1024 estimated HAL transient

  - name: HabitTracker        # wip; fixed habits plus encoded record in object
    tier: c3
    static_bytes: 0
    object_bytes: 1536        # conservative estimate
    peak_heap_bytes: 8192     # estimated shared keyboard/HAL transient, unverified
